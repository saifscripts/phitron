#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v1 = {1, 3, 4, 5, 4, 5, 4};

    for (auto it = v1.begin(); it < v1.end(); it++)
    {
        cout << *it << " ";
    }

    return 0;
}